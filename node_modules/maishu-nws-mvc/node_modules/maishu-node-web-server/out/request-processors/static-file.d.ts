import { RequestProcessor, RequestContext, RequestResult } from "../request-processor";
export declare class StaticFileRequestProcessor implements RequestProcessor {
    priority: number;
    private _contentTypes;
    private _staticPath;
    private _ignorePaths;
    constructor();
    /** 获取内容类型 */
    get contentTypes(): {
        [key: string]: string;
    };
    /** 设置内容类型 */
    set contentTypes(value: {
        [key: string]: string;
    });
    /** 获取静态文件夹路径 */
    get staticPath(): string | null | undefined;
    /** 设置静态文件夹路径，如果路径位空，则位网站根目录 */
    set staticPath(value: string | null | undefined);
    /** 获取忽略的路径 */
    get ignorePaths(): string[];
    /** 设置忽略的路径（正则表达式），如果某些请求的路径不需要处理，设置此参数。 */
    set ignorePaths(value: string[]);
    execute(ctx: RequestContext): Promise<RequestResult | null>;
    private processStaticFile;
}
