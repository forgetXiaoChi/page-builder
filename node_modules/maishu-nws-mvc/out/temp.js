"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
fs.watch("d:/projects/temp").on("change", (event, path) => {
    // if (event != "add" && event != "change" && event != "unlink")
    //     return;
    console.log(new Date(Date.now()));
    console.log(`${event} ${path}`);
});
