import { DataSource } from "maishu-toolkit";
export declare class ArrayDataSource<T> extends DataSource<T> {
    #private;
    constructor(dataItems: T[], primaryKeys?: (keyof T)[]);
    get dataItems(): T[];
    set dataItems(value: T[]);
    private findDataItem;
    private samePrimaryKeyValues;
}
