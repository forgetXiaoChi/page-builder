import { DataControlField } from "../fields/DataControlField";
import { Callback } from "maishu-toolkit";
import { GridViewCell } from "./GridViewCell";
export declare class GridViewHeaderCell<T> extends GridViewCell {
    private _sortType;
    private _iconElement;
    private field;
    ascHTML: string;
    descHTML: string;
    sortingHTML: string;
    toSortHTML: string;
    sorting: Callback<{
        sortType: string;
    }>;
    sorted: Callback<{
        sortType: string;
    }>;
    constructor(field: DataControlField<T>, cellElement: HTMLElement);
    handleSort(): Promise<void>;
    private defaultHeaderText;
    get sortType(): "desc" | "asc";
    set sortType(value: "desc" | "asc");
    clearSortIcon(): void;
    private updateSortIcon;
}
