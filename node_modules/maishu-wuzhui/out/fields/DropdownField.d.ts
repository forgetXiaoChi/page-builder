import { BoundFieldParams, BoundField } from "./BoundField";
import { DataSource } from "maishu-toolkit";
import { GridViewEditableCell } from "../cells/GridViewEditableCell";
export interface DropdownFieldParams<T, S> extends BoundFieldParams<T> {
    dataSource: DataSource<S>;
    emptyText?: string;
    nameField: keyof S;
    valueField: keyof S;
}
export declare class DropdownField<T, S> extends BoundField<T, DropdownFieldParams<T, S>> {
    #private;
    constructor(params: DropdownFieldParams<T, S>);
    private findDataItem;
    private samePrimaryKeyValues;
    private getDataItems;
    createControl(): {
        element: HTMLSelectElement;
        value: string;
    };
    createItemCell(dataItem: T, cellElement: HTMLElement): GridViewEditableCell<T>;
}
