#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../maishu-node-mvc/cli/static.js" "$@"
  ret=$?
else 
  node  "$basedir/../maishu-node-mvc/cli/static.js" "$@"
  ret=$?
fi
exit $ret
