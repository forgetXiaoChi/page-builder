import * as w from 'maishu-wuzhui';
export interface CheckboxListFieldParams<T, S> extends w.BoundFieldParams<T> {
    dataSource: w.DataSource<S>;
    emptyText?: string;
    nameField: keyof S;
    valueField: keyof S;
}
export declare function checkboxListField<T, S>(params: CheckboxListFieldParams<T, S>): CheckboxListField<T, S>;
declare class CheckboxListField<T, S> extends w.BoundField<T> {
    private _params;
    private checkboxs;
    constructor(params: CheckboxListFieldParams<T, S>);
    getDataItems(): Promise<S[]>;
    createControl(): {
        element: HTMLDivElement;
        value: string[];
    };
}
export {};
