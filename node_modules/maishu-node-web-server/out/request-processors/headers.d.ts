import { RequestContext, RequestProcessor } from "..";
import { RequestResult } from "../request-processor";
export declare type Headers = {
    [name: string]: string | string[];
};
interface Options {
    headers: Headers;
}
export declare class HeadersRequestProcessor implements RequestProcessor<Options> {
    options: Options;
    priority: number;
    constructor();
    get headers(): Headers;
    set headers(value: Headers);
    execute(ctx: RequestContext): RequestResult | null;
}
export {};
