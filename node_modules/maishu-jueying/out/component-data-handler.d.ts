/// <reference types="react" />
import { ComponentData } from "maishu-jueying-core";
import { Callback } from "maishu-toolkit";
declare type Components = {
    [typeName: string]: React.Component[];
};
export declare class ComponentDataHandler {
    private _pageData;
    private _components;
    componentSelected: Callback<string[]>;
    componentRemoved: Callback<string[]>;
    componentAppend: Callback<ComponentDataHandler>;
    componentUpdated: Callback<ComponentData[]>;
    pageDataChanged: Callback<ComponentData>;
    constructor(componentData: ComponentData);
    /** 对 pageData 字段补全 */
    private initComponentData;
    /** 获取已选择了的组件 */
    get selectedComponents(): ComponentData[];
    /** 获取已选择了的组件编号 */
    get selectedComponentIds(): string[];
    /**  */
    get components(): Components;
    get pageData(): ComponentData;
    set pageData(value: ComponentData);
    /**
     * 移动控件到另外一个控件容器
     * @param componentId 要移动的组件编号
     * @param parentId 目标组件编号
     * @param beforeChildId 组件的前一个子组件编号
     */
    moveComponent(componentId: string, parentId: string, childComponentIndex?: number): void;
    updateComponentProps(componentProps: {
        componentId: string;
        propName: string;
        value: any;
    }[]): any;
    findComponentData(componentId: string): ComponentData | null;
    private removeComponentFrom;
    private static travelComponentData;
    /**
     * 添加控件
     * @param parentId 父控件编号
     * @param componentData 控件数据
     * @param componentIndex 新添加组件在子组件中的次序
     */
    appendComponent(parentId: string, componentData: ComponentData, componentIndex?: number): void;
    /**
     * 对组件及其子控件进行命名
     * @param component
     */
    private static fillComponent;
    private static setComponetRefProp;
    /**
     * 选择指定的控件
     * @param control 指定的控件
     */
    selectComponent(componentId: string): void;
    /**
     * 选择指定的控件，一个或多个
     * @param control 指定的控件
     */
    selectComponents(componentIds: string[] | string): void;
    removeComponent(componentId: string): void;
    /** 移除控件 */
    removeComponents(componentIds: string[]): void;
}
export {};
