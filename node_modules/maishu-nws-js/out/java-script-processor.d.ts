import { RequestContext, RequestProcessor, RequestResult } from "maishu-node-web-server";
import * as babel from "@babel/core";
interface Options {
    babel: {
        [key: string]: babel.TransformOptions;
    };
    directoryPath?: string;
    ignorePaths: string[];
}
export declare class JavaScriptProcessor implements RequestProcessor<Options> {
    options: Options;
    get babelOptions(): {
        [key: string]: babel.TransformOptions;
    };
    set babelOptions(value: {
        [key: string]: babel.TransformOptions;
    });
    get ignorePaths(): string[];
    set ignorePaths(value: string[]);
    get basePath(): string;
    set basePath(value: string);
    execute(ctx: RequestContext): Promise<RequestResult | null>;
    static transformJS(originalCode: string, options: babel.TransformOptions): any;
    static transformTS(originalCode: string, options: babel.TransformOptions): any;
}
export {};
