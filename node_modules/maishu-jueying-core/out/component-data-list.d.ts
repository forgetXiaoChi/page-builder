import { ComponentData } from "./component-data";
declare type Item = Omit<ComponentData, "children"> & {
    id: string;
    parentId: string | null;
};
export declare type ComponentDataList = Item[];
export {};
